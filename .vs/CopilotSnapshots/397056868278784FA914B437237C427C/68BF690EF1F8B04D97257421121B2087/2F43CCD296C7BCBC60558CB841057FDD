{% extends 'base.html' %}
{% block title %}API Documentation - Panel{% endblock %}
{% block content %}
<div class="container">
    <div class="panel">
        <h1>📚 API Documentation</h1>
        <p class="text-secondary">Interactive API documentation for the Panel application</p>

        <div class="api-docs-tabs">
            <div class="tabs">
                <button class="tab-button active" onclick="showTab('swagger')">Swagger UI</button>
                <button class="tab-button" onclick="showTab('redoc')">ReDoc</button>
                <button class="tab-button" onclick="showTab('endpoints')">Endpoints</button>
            </div>

            <div class="tab-content">
                <!-- Swagger UI Tab -->
                <div id="swagger-tab" class="tab-pane active">
                    <div class="api-info">
                        <h3>Interactive API Explorer</h3>
                        <p>Test API endpoints directly from your browser. Click "Try it out" on any endpoint to make live requests.</p>
                        <div class="api-features">
                            <span class="feature-tag">🔐 Authentication</span>
                            <span class="feature-tag">📝 Interactive Testing</span>
                            <span class="feature-tag">📋 Request/Response Examples</span>
                            <span class="feature-tag">🎯 Schema Validation</span>
                        </div>
                    </div>
                    <div id="swagger-ui-container">
                        <div class="loading">Loading API documentation...</div>
                    </div>
                </div>

                <!-- ReDoc Tab -->
                <div id="redoc-tab" class="tab-pane">
                    <div class="api-info">
                        <h3>ReDoc Documentation</h3>
                        <p>Clean, responsive API documentation with three-panel layout.</p>
                    </div>
                    <div id="redoc-container">
                        <div class="loading">Loading ReDoc documentation...</div>
                    </div>
                </div>

                <!-- Endpoints Tab -->
                <div id="endpoints-tab" class="tab-pane">
                    <div class="api-info">
                        <h3>API Endpoints Overview</h3>
                        <p>Quick reference of all available API endpoints.</p>
                    </div>

                    <div class="endpoints-grid">
                        <!-- Authentication -->
                        <div class="endpoint-section">
                            <h4>🔐 Authentication</h4>
                            <div class="endpoint-list">
                                <div class="endpoint-item">
                                    <code>POST /api/v1/auth/login</code>
                                    <span class="method post">POST</span>
                                    <p>User login</p>
                                </div>
                                <div class="endpoint-item">
                                    <code>POST /api/v1/auth/register</code>
                                    <span class="method post">POST</span>
                                    <p>User registration</p>
                                </div>
                                <div class="endpoint-item">
                                    <code>POST /api/v1/auth/logout</code>
                                    <span class="method post">POST</span>
                                    <p>User logout</p>
                                </div>
                                <div class="endpoint-item">
                                    <code>GET /api/v1/auth/profile</code>
                                    <span class="method get">GET</span>
                                    <p>Get user profile</p>
                                </div>
                            </div>
                        </div>

                        <!-- Users -->
                        <div class="endpoint-section">
                            <h4>👥 User Management</h4>
                            <div class="endpoint-list">
                                <div class="endpoint-item">
                                    <code>GET /api/v1/users/</code>
                                    <span class="method get">GET</span>
                                    <p>List users</p>
                                </div>
                                <div class="endpoint-item">
                                    <code>GET /api/v1/users/{id}</code>
                                    <span class="method get">GET</span>
                                    <p>Get user details</p>
                                </div>
                                <div class="endpoint-item">
                                    <code>PUT /api/v1/users/{id}</code>
                                    <span class="method put">PUT</span>
                                    <p>Update user</p>
                                </div>
                            </div>
                        </div>

                        <!-- Forum -->
                        <div class="endpoint-section">
                            <h4>💬 Forum</h4>
                            <div class="endpoint-list">
                                <div class="endpoint-item">
                                    <code>GET /api/v1/forum/threads</code>
                                    <span class="method get">GET</span>
                                    <p>List threads</p>
                                </div>
                                <div class="endpoint-item">
                                    <code>POST /api/v1/forum/threads</code>
                                    <span class="method post">POST</span>
                                    <p>Create thread</p>
                                </div>
                                <div class="endpoint-item">
                                    <code>GET /api/v1/forum/threads/{id}/posts</code>
                                    <span class="method get">GET</span>
                                    <p>Get thread posts</p>
                                </div>
                            </div>
                        </div>

                        <!-- GDPR -->
                        <div class="endpoint-section">
                            <h4>🛡️ GDPR</h4>
                            <div class="endpoint-list">
                                <div class="endpoint-item">
                                    <code>POST /api/v1/gdpr/export</code>
                                    <span class="method post">POST</span>
                                    <p>Request data export</p>
                                </div>
                                <div class="endpoint-item">
                                    <code>POST /api/v1/gdpr/delete</code>
                                    <span class="method post">POST</span>
                                    <p>Request account deletion</p>
                                </div>
                            </div>
                        </div>

                        <!-- Health -->
                        <div class="endpoint-section">
                            <h4>❤️ Health Checks</h4>
                            <div class="endpoint-list">
                                <div class="endpoint-item">
                                    <code>GET /api/v1/health/</code>
                                    <span class="method get">GET</span>
                                    <p>System health</p>
                                </div>
                                <div class="endpoint-item">
                                    <code>GET /api/v1/health/detailed</code>
                                    <span class="method get">GET</span>
                                    <p>Detailed health</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="api-info-section">
            <h3>🚀 Getting Started</h3>
            <div class="getting-started">
                <div class="step">
                    <span class="step-number">1</span>
                    <div class="step-content">
                        <h4>Authentication</h4>
                        <p>Use the login endpoint to get an authentication token, then include it in the Authorization header for authenticated requests.</p>
                        <code>Authorization: Bearer your_token_here</code>
                    </div>
                </div>

                <div class="step">
                    <span class="step-number">2</span>
                    <div class="step-content">
                        <h4>API Base URL</h4>
                        <p>All API endpoints are prefixed with <code>/api/v1/</code></p>
                        <code>Base URL: https://yourdomain.com/api/v1/</code>
                    </div>
                </div>

                <div class="step">
                    <span class="step-number">3</span>
                    <div class="step-content">
                        <h4>Response Format</h4>
                        <p>All responses are in JSON format with consistent error handling.</p>
                        <pre><code>{
  "success": true,
  "data": {...},
  "message": "Operation successful"
}</code></pre>
                    </div>
                </div>
            </div>
        </div>

        <div class="api-links">
            <h3>🔗 Additional Resources</h3>
            <div class="resource-links">
                <a href="/api/v1/swagger.json" class="btn btn-secondary">
                    📄 OpenAPI JSON
                </a>
                <a href="https://github.com/phillgates2/panel" class="btn btn-secondary">
                    📚 GitHub Repository
                </a>
                <a href="/docs" class="btn btn-secondary">
                    📖 Full Documentation
                </a>
            </div>
        </div>
    </div>
</div>

<style>
.api-docs-tabs {
    margin-top: 2rem;
}

.tabs {
    display: flex;
    border-bottom: 2px solid var(--border);
    margin-bottom: 2rem;
}

.tab-button {
    padding: 1rem 2rem;
    border: none;
    background: none;
    color: var(--text-secondary);
    font-weight: 600;
    cursor: pointer;
    border-bottom: 3px solid transparent;
    transition: all 0.3s ease;
}

.tab-button.active {
    color: var(--accent);
    border-bottom-color: var(--accent);
}

.tab-button:hover {
    color: var(--accent);
}

.tab-pane {
    display: none;
}

.tab-pane.active {
    display: block;
}

.api-info {
    margin-bottom: 2rem;
    padding: 1.5rem;
    background: var(--panel-hover);
    border-radius: 8px;
    border: 1px solid var(--border);
}

.api-features {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-top: 1rem;
}

.feature-tag {
    background: var(--accent);
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 500;
}

#swagger-ui-container,
#redoc-container {
    border: 2px solid var(--border);
    border-radius: 8px;
    overflow: hidden;
}

.loading {
    padding: 3rem;
    text-align: center;
    color: var(--text-secondary);
    font-size: 1.1rem;
}

.endpoints-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
}

.endpoint-section {
    background: var(--panel-hover);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 1.5rem;
}

.endpoint-section h4 {
    margin: 0 0 1rem 0;
    color: var(--accent);
    font-size: 1.2rem;
}

.endpoint-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

.endpoint-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.75rem;
    background: var(--panel);
    border-radius: 6px;
    border: 1px solid var(--border-subtle);
}

.endpoint-item code {
    flex: 1;
    font-family: 'Courier New', monospace;
    font-size: 0.9rem;
    background: transparent;
    padding: 0;
    border: none;
}

.method {
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-size: 0.75rem;
    font-weight: bold;
    text-transform: uppercase;
}

.method.get {
    background: #28a745;
    color: white;
}

.method.post {
    background: #007bff;
    color: white;
}

.method.put {
    background: #ffc107;
    color: black;
}

.method.delete {
    background: #dc3545;
    color: white;
}

.endpoint-item p {
    margin: 0;
    font-size: 0.9rem;
    color: var(--text-secondary);
}

.getting-started {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-top: 1rem;
}

.step {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
}

.step-number {
    background: var(--accent);
    color: white;
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    flex-shrink: 0;
}

.step-content h4 {
    margin: 0 0 0.5rem 0;
    color: var(--accent);
}

.step-content p {
    margin: 0 0 0.5rem 0;
    color: var(--text-secondary);
}

.step-content code {
    background: var(--panel-hover);
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-family: 'Courier New', monospace;
    font-size: 0.9rem;
}

.step-content pre {
    background: var(--panel-hover);
    padding: 1rem;
    border-radius: 6px;
    overflow-x: auto;
    margin: 0.5rem 0 0 0;
}

.api-links {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 2px solid var(--border);
}

.resource-links {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 1rem;
}

@media (max-width: 768px) {
    .tabs {
        flex-direction: column;
    }

    .tab-button {
        border-bottom: none;
        border-left: 3px solid transparent;
    }

    .tab-button.active {
        border-bottom: none;
        border-left-color: var(--accent);
    }

    .endpoints-grid {
        grid-template-columns: 1fr;
    }

    .endpoint-item {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
    }

    .step {
        flex-direction: column;
        gap: 0.75rem;
    }

    .resource-links {
        flex-direction: column;
    }

    .resource-links .btn {
        width: 100%;
        text-align: center;
    }
}
</style>

<script>
// Tab switching functionality
function showTab(tabName) {
    // Hide all tab panes
    const tabPanes = document.querySelectorAll('.tab-pane');
    tabPanes.forEach(pane => pane.classList.remove('active'));

    // Remove active class from all tab buttons
    const tabButtons = document.querySelectorAll('.tab-button');
    tabButtons.forEach(button => button.classList.remove('active'));

    // Show selected tab
    document.getElementById(tabName + '-tab').classList.add('active');
    event.target.classList.add('active');

    // Load content for specific tabs
    if (tabName === 'swagger') {
        loadSwaggerUI();
    } else if (tabName === 'redoc') {
        loadReDoc();
    }
}

// Load Swagger UI
function loadSwaggerUI() {
    const container = document.getElementById('swagger-ui-container');

    // Check if already loaded
    if (container.querySelector('.swagger-ui')) {
        return;
    }

    // Load Swagger UI CSS and JS
    const swaggerCSS = document.createElement('link');
    swaggerCSS.rel = 'stylesheet';
    swaggerCSS.href = 'https://unpkg.com/swagger-ui-dist@5.7.2/swagger-ui.css';
    document.head.appendChild(swaggerCSS);

    const swaggerJS = document.createElement('script');
    swaggerJS.src = 'https://unpkg.com/swagger-ui-dist@5.7.2/swagger-ui-bundle.js';
    swaggerJS.onload = function() {
        SwaggerUIBundle({
            url: '/api/v1/swagger.json',
            dom_id: '#swagger-ui-container',
            deepLinking: true,
            presets: [
                SwaggerUIBundle.presets.apis,
                SwaggerUIBundle.presets.standalone
            ],
            plugins: [
                SwaggerUIBundle.plugins.DownloadUrl
            ],
            layout: "BaseLayout",
            tryItOutEnabled: true,
            requestInterceptor: function(req) {
                // Add auth token if available
                const token = localStorage.getItem('api_token');
                if (token) {
                    req.headers.Authorization = 'Bearer ' + token;
                }
                return req;
            }
        });
    };
    document.body.appendChild(swaggerJS);
}

// Load ReDoc
function loadReDoc() {
    const container = document.getElementById('redoc-container');

    // Check if already loaded
    if (container.querySelector('redoc')) {
        return;
    }

    // Load ReDoc
    const redocJS = document.createElement('script');
    redocJS.src = 'https://cdn.redoc.ly/redoc/latest/bundles/redoc.standalone.js';
    redocJS.onload = function() {
        Redoc.init('/api/v1/swagger.json', {
            scrollYOffset: 50,
            hideDownloadButton: false,
            hideHostname: false,
            theme: {
                colors: {
                    primary: {
                        main: '#1f6feb'
                    }
                }
            }
        }, document.getElementById('redoc-container'));
    };
    document.body.appendChild(redocJS);
}

// Initialize first tab
document.addEventListener('DOMContentLoaded', function() {
    showTab('swagger');
});

// API token management
function setApiToken(token) {
    localStorage.setItem('api_token', token);
    alert('API token saved! It will be included in all API requests.');
}

function clearApiToken() {
    localStorage.removeItem('api_token');
    alert('API token cleared.');
}

// Add token management to page
document.addEventListener('DOMContentLoaded', function() {
    const tokenSection = document.createElement('div');
    tokenSection.innerHTML = `
        <div style="margin-top: 2rem; padding: 1rem; background: var(--panel-hover); border-radius: 8px; border: 1px solid var(--border);">
            <h4>🔑 API Token Management</h4>
            <p>Enter your API token to authenticate requests in the interactive documentation:</p>
            <div style="display: flex; gap: 0.5rem; margin-top: 0.5rem;">
                <input type="password" id="api-token" placeholder="Enter API token" style="flex: 1; padding: 0.5rem; border: 1px solid var(--border); border-radius: 4px; background: var(--panel); color: var(--text);">
                <button onclick="setApiToken(document.getElementById('api-token').value)" class="btn btn-primary btn-sm">Save Token</button>
                <button onclick="clearApiToken()" class="btn btn-secondary btn-sm">Clear Token</button>
            </div>
        </div>
    `;

    const apiInfo = document.querySelector('.api-info');
    if (apiInfo) {
        apiInfo.appendChild(tokenSection);
    }
});
</script>
{% endblock %}
