# Testing Dependencies for Panel Application
# Install after installing requirements-prod.txt

# Core testing framework
pytest==9.0.1
pytest-asyncio==0.21.1

# Browser automation testing (requires playwright installation)
# Note: playwright requires additional system setup
# Run: playwright install chromium
pytest-playwright==0.7.1
