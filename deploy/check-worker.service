[Unit]
Description=Check RQ worker and restart if needed
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/bin/env bash /opt/panel/tools/check_worker.sh rq-worker.service
User=root

[Install]
WantedBy=multi-user.target
